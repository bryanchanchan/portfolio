<?xml version="1.0" encoding="UTF-8"?>

<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">

  <url>
    <!-- The <loc> tag is the only one that’s required. -->
    <loc>https://chanchan-design.com</loc>
    <!-- OPTIONAL: Priority will default to 0.5 but can go up to 1.0. -->
    <priority>1.0</priority>
    <!-- OPTIONAL: Must be in this format -->
    <lastmod>2018-11-28</lastmod>
    <!-- OPTIONAL: can be always, hourly, daily, weekly, monthly, yearly, never -->
    <changefreq>yearly</changefreq>
  </url>

  <url>
    <!-- The <loc> tag is the only one that’s required. -->
    <loc>https://chanchan-design.com/branding</loc>
    <!-- OPTIONAL: Priority will default to 0.5 but can go up to 1.0. -->
    <priority>1.0</priority>
    <!-- OPTIONAL: Must be in this format -->
    <lastmod>2018-11-28</lastmod>
    <!-- OPTIONAL: can be always, hourly, daily, weekly, monthly, yearly, never -->
    <changefreq>yearly</changefreq>
  </url>

  <url>
    <!-- The <loc> tag is the only one that’s required. -->
    <loc>https://chanchan-design.com/campaign</loc>
    <!-- OPTIONAL: Priority will default to 0.5 but can go up to 1.0. -->
    <priority>1.0</priority>
    <!-- OPTIONAL: Must be in this format -->
    <lastmod>2018-11-28</lastmod>
    <!-- OPTIONAL: can be always, hourly, daily, weekly, monthly, yearly, never -->
    <changefreq>yearly</changefreq>
  </url>

  <url>
    <!-- The <loc> tag is the only one that’s required. -->
    <loc>https://chanchan-design.com/illustration</loc>
    <!-- OPTIONAL: Priority will default to 0.5 but can go up to 1.0. -->
    <priority>1.0</priority>
    <!-- OPTIONAL: Must be in this format -->
    <lastmod>2018-11-28</lastmod>
    <!-- OPTIONAL: can be always, hourly, daily, weekly, monthly, yearly, never -->
    <changefreq>yearly</changefreq>
  </url>

  <url>
    <!-- The <loc> tag is the only one that’s required. -->
    <loc>https://chanchan-design.com/poster</loc>
    <!-- OPTIONAL: Priority will default to 0.5 but can go up to 1.0. -->
    <priority>1.0</priority>
    <!-- OPTIONAL: Must be in this format -->
    <lastmod>2018-11-28</lastmod>
    <!-- OPTIONAL: can be always, hourly, daily, weekly, monthly, yearly, never -->
    <changefreq>yearly</changefreq>
  </url>

</urlset>
